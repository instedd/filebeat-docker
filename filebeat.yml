filebeat.inputs:
- type: docker
  containers.ids: '*'
  processors:
  - add_docker_metadata: ~
  ignore_older: 30m

output.elasticsearch:
  hosts: ${ELASTICSEARCH_URL}
  compression_level: 1

setup.template.settings:
  index:
    number_of_shards: 1
    number_of_replicas: 0
